<% layout("/layouts/boilerplate") %>

  <section id="show-page">
    <div class="container show-container">

      <!-- IMAGE SLIDER -->
      <div class="show-slider">
        <% listing.image.forEach((img, index)=> { %>
          <img src="<%= img.url %>" class="<%= index === 0 ? 'active' : '' %>">
          <% }) %>
            <button class="slider-btn prev-btn" onclick="changeSlide(-1)">&#10094;</button>
            <button class="slider-btn next-btn" onclick="changeSlide(1)">&#10095;</button>
      </div>

      <!-- CONTENT -->
      <div class="show-content">
        <h1 class="show-title">
          <%= listing.title %>
        </h1>
        <p class="show-description">
          <%= listing.description %>
        </p>
        <p class="show-price">₹ <%= listing.price.toLocaleString("en-IN") %> <span>/night</span></p>
        <p class="show-location">📍 <%= listing.location %>
        </p>
        <p class="show-country">🌍 <%= listing.country %>
        </p>

        <a href="/listings/<%= listing._id %>/booking" class="btn-book">Book Now</a>


        <% if (currentUser && currentUser.email===listing.useremail || currentUser.email==="milspatel21@gmail.com" ) {
          %>
          <div class="show-actions">
            <a class="btn-edit" href="/listings/<%= listing._id %>/edit">Edit</a>
            <a class="btn-delete" href="/listings/<%= listing._id %>/delete">Delete</a>
          </div>
          <% } %>

            <br>
            <a class="btn-back" href="/listings">Back</a>
      </div>


<!-- Trigger Button -->
<button class="btn btn-outline-dark" data-bs-toggle="modal" data-bs-target="#amenitiesModal">
  Show all amenities
</button>

<!-- Modal -->
<div class="modal fade" id="amenitiesModal" tabindex="-1">
  <div class="modal-dialog modal-xl modal-dialog-centered">
    <div class="modal-content amenities-modal">

      <!-- Header -->
      <div class="modal-header border-0 px-4 pt-4">
        <h3 class="modal-title fw-bold">What this place offers</h3>
        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
      </div>

      <!-- Scrollable Body -->
      <div class="modal-body amenities-body px-4">

        <!-- Scenic & Views -->
        <h4 class="fw-bold mt-4">Scenic & Views</h4>
        <div class="row g-4 mb-4">
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-sun fs-2"></i>
            <span class="fs-5">Sunset view</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-stars fs-2"></i>
            <span class="fs-5">Starry night view</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-tree fs-2"></i>
            <span class="fs-5">Forest view</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-water fs-2"></i>
            <span class="fs-5">Lake view</span>
          </div>
        </div>

        <hr>

        <!-- Essentials -->
        <h4 class="fw-bold mt-4">Essentials</h4>
        <div class="row g-4 mb-4">
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-wifi fs-2"></i>
            <span class="fs-5">High-speed Wifi</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-lightning fs-2"></i>
            <span class="fs-5">24/7 Power Backup</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-droplet fs-2"></i>
            <span class="fs-5">Hot water</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-cup-hot fs-2"></i>
            <span class="fs-5">Kitchen essentials</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-shop fs-2"></i>
            <span class="fs-5">Nearby groceries</span>
          </div>
        </div>

        <hr>

        <!-- Comfort & Safety -->
        <h4 class="fw-bold mt-4">Comfort & Safety</h4>
        <div class="row g-4 mb-4">
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-fan fs-2"></i>
            <span class="fs-5">Ceiling fans</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-thermometer fs-2"></i>
            <span class="fs-5">Heater</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-shield-lock fs-2"></i>
            <span class="fs-5">Smart lock</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-shield-check fs-2"></i>
            <span class="fs-5">Security system</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-fire fs-2"></i>
            <span class="fs-5">Fire extinguisher</span>
          </div>
        </div>

        <hr>

        <!-- Family & Kids -->
        <h4 class="fw-bold mt-4">Family & Kids</h4>
        <div class="row g-4 mb-4">
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="fas fa-baby"></i>
            <span class="fs-5">Crib</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-person fs-2"></i>
            <span class="fs-5">Child-friendly environment</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-basket fs-2"></i>
            <span class="fs-5">Toy collection</span>
          </div>
        </div>

        <hr>

        <!-- Outdoor & Recreation -->
        <h4 class="fw-bold mt-4">Outdoor & Recreation</h4>
        <div class="row g-4 mb-4">
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-bicycle fs-2"></i>
            <span class="fs-5">Bicycle rental</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
    <i class="fas fa-person-swimming"></i>
            <span class="fs-5">Swimming pool</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="fas fa-child"></i>
            <span class="fs-5">Playground</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-tree fs-2"></i>
            <span class="fs-5">Garden/Backyard</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-cup-straw fs-2"></i>
            <span class="fs-5">BBQ grill</span>
          </div>
        </div>

        <hr>

        <!-- Entertainment & Work -->
        <h4 class="fw-bold mt-4">Entertainment & Work</h4>
        <div class="row g-4 mb-4">
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-tv fs-2"></i>
            <span class="fs-5">Smart TV</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-laptop fs-2"></i>
            <span class="fs-5">Work desk</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-speaker fs-2"></i>
            <span class="fs-5">Bluetooth speaker</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-camera-video fs-2"></i>
            <span class="fs-5">Projector</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
           <i class="fas fa-gamepad fs-2"></i>

            <span class="fs-5">Gaming console</span>
          </div>
        </div>

        <hr>

        <!-- Accessibility & Transport -->
        <h4 class="fw-bold mt-4">Accessibility & Transport</h4>
        <div class="row g-4 mb-4">
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-door-open fs-2"></i>
            <span class="fs-5">Wheelchair accessible</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-car-front fs-2"></i>
            <span class="fs-5">Free parking</span>
          </div>
          <div class="col-md-6 d-flex align-items-center gap-3 amenity-item">
            <i class="bi bi-bus-front fs-2"></i>
            <span class="fs-5">Nearby public transport</span>
          </div>
        </div>

        <!-- This brings total to ~45 unique amenities -->

      </div>
    </div>
  </div>
</div>





    </div>


    <div class="map-wrapper">
      <div class="map-card">
        <h3 class="map-heading">Where you'll be</h3>
        <div id="listing-map"></div>
      </div>
    </div>


    <div class="show-reviews-section">
      <% if(currentUser) { %>
        <h4>Leave a Review</h4>
        <form action="/listings/<%= listing.id %>/reviews" method="POST" novalidate class="needs-validation">

          <div class="mb-3 mt-3">
            <label for="name" class="form-label">Name</label>
            <input type="text" value="<%= currentUser.name %>" id="name" name="review[name]" class="form-control"
              required>

            <label for="rating" class="form-label">Rating</label>
            <div class="star-rating">
              <input type="radio" id="star5" name="review[rating]" value="5">
              <label for="star5"></label>

              <input type="radio" id="star4" name="review[rating]" value="4">
              <label for="star4"></label>

              <input type="radio" id="star3" name="review[rating]" value="3">
              <label for="star3"></label>

              <input type="radio" id="star2" name="review[rating]" value="2">
              <label for="star2"></label>

              <input type="radio" id="star1" name="review[rating]" value="1" required>
              <label for="star1"></label>


            </div>
            <p id="ratingValue">Rating: 0</p>
            <br>
            <label for="comment" class="form-label">Comments</label>
            <textarea name="review[comment]" id="comment" cols="30" rows="5" class="form-control" required></textarea>
          </div>
          <button class="btn btn-outline-dark">Submit</button>
        </form>
        <hr>
        <% } %>

          <% if(listing.reviews.length> 0) { %>
            <p><b>All Reviews</b></p>
            <div class="row">
              <% for(review of listing.reviews) { %>
                <div class="card col-12 col-md-5 ms-md-3 mb-3">
                  <div class="card-body">
                    <h5 class="card-title">@<%= review.name %>
                    </h5>
                    <div class="star-rating" data-rating="<%= review.rating %>">
                      <span data-value="5">★</span>
                      <span data-value="4">★</span>
                      <span data-value="3">★</span>
                      <span data-value="2">★</span>
                      <span data-value="1">★</span>
                    </div>

                    <p class="text-muted small mb-2">
                      <%= new Date(review.createdAt).toLocaleDateString("en-US", { year: 'numeric' , month: 'long' ,
                        day: 'numeric' }) %>
                    </p>

                    <p class="card-text">
                      <%= review.comment %>
                    </p>
                    <% if(currentUser && currentUser.name==review.name) { %>
                      <form class="mb-3" method="GET"
                        action="/listings/<%= listing._id %>/reviews/<%= review._id %>/delete">
                        <button class="btn btn-sm btn-dark">Delete</button>
                      </form>
                      <% } %>
                  </div>
                </div>
                <% } %>
            </div>
            <% } %>
    </div>


    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
      integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" />
    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
      integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>
    <script>
      const listingLocation = "<%= listing.location %>";
      const listingCountry = "<%= listing.country %>";

      // Initialize map
      const map = L.map('listing-map').setView([20.5937, 78.9629], 5); // Default to India

      L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
        attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
      }).addTo(map);

      // Fetch coordinates from Nominatim (Free OpenStreetMap Geocoder)
      const query = `${listingLocation}, ${listingCountry}`;
      fetch(`https://nominatim.openstreetmap.org/search?format=json&q=${encodeURIComponent(query)}`)
        .then(response => response.json())
        .then(data => {
          if (data && data.length > 0) {
            const lat = parseFloat(data[0].lat);
            const lon = parseFloat(data[0].lon);

            map.setView([lat, lon], 13);

            L.marker([lat, lon]).addTo(map)
              .bindPopup(`<b>${listingLocation}</b><br>Exact location provided after booking.`)
              .openPopup();

            // Add a circle to show approximate area
            L.circle([lat, lon], {
              color: '#ff385c',
              fillColor: '#ff385c',
              fillOpacity: 0.2,
              radius: 500
            }).addTo(map);

          } else {
            console.log("Location not found");
          }
        })
        .catch(err => console.error("Error fetching location:", err));
    </script>
  </section>

  <script>
    // IMAGE SLIDER
    const slides = document.querySelectorAll('.show-slider img');
    let current = 0;

    // Show first slide initially
    if (slides.length > 0) slides[0].classList.add('active');

    function changeSlide(direction) {
      if (slides.length === 0) return;

      slides[current].classList.remove('active');

      current = current + direction;

      // Loop boundaries
      if (current >= slides.length) {
        current = 0;
      } else if (current < 0) {
        current = slides.length - 1;
      }

      slides[current].classList.add('active');
    }

    // Auto-play slider
    setInterval(() => changeSlide(1), 3000);

    document.querySelectorAll('.star-rating').forEach(rating => {
      const value = Number(rating.dataset.rating) || 0

      rating.querySelectorAll('span').forEach(star => {
        if (star.dataset.value <= value) {
          star.classList.add('active')
        }
      })
    })



    const stars = document.querySelectorAll('input[name="review[rating]"]');
    const display = document.getElementById("ratingValue");

    stars.forEach(star => {
      star.addEventListener("change", () => {
        display.textContent = "Rating: " + star.value;
      });
    });
  </script>